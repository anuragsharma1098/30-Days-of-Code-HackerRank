#include <bits/stdc++.h>
using namespace std;
class print
{
    public:
    void pr(int n)
    {
        for(int i=1;i<=10;i++)
        cout<<n<<" x "<<i<<" = "<<n*i<<endl;
    };
  
};
int main()
{
 int n;
 print p;
 cin>>n;
 if(n>=2 && n<=20)
 p.pr(n);  
    return 0;
}

   
